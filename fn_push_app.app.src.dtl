{application, {{name}}, [
    {description, "WebSocket, Event Stream and COMET {{name}} Application."},
    {vsn, "1"},
    {modules, []},
    {registered, [{{name}}_sup]},
    {applications, [
        kernel,
        stdlib,
        cowboy,
        bullet,
        lager
    ]},
    {mod, { {{name}}_app, []}},
    {env, []}
]}.
